import { FiltroFotoPipe } from './filtro-foto.pipe';

describe('FiltroFotoPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroFotoPipe();
    expect(pipe).toBeTruthy();
  });
});
